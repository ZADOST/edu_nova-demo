name: edu_nova
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Added essential packages for ZAS TECH architecture
  go_router: ^13.2.0
  shared_preferences: ^2.2.2
  dio: ^5.4.0
  qr_flutter: ^4.0.0
  mobile_scanner: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true